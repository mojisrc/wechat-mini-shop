<view class="search">
    <page-goods-search
            bind:click="onSearchClick"
    />
</view>
<page-base
        wx:if="{{body.length>0}}"
        id="page"
        body="{{body}}"
        backgroundColor="{{backgroundColor}}"
        login="{{login}}"
/>
